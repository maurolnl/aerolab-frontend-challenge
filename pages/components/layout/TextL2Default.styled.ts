import styled from "styled-components";

import {Colors, TextStyles} from "../../../styles/Theme";

export const TextL2Default = styled.p`
  color: ${Colors.Neutral[100]};
  ${TextStyles.Bundler(TextStyles.Texts.L2.Default)};
`;
